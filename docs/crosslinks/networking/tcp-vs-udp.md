# Cross-links wanted from `networking/tcp-vs-udp`

Targets are unpublished (not yet in the build) as of this writing, so the article states these plainly in prose instead of linking. Wire in once each target is published.

- Anchor text "HTTP/1.1" (in "Framing a stream yourself" section, sentence beginning "This is what every text protocol built on TCP does one way or another") → `/networking/http-explained/`
- Anchor text "chunked encoding" (same sentence) → `/networking/http-explained/#chunked-transfer` or the relevant heading once that article exists
- Anchor text "A single DNS query" (in "What the guarantees cost in bytes" section) → `/networking/dns/`
- Anchor text "a resolver's first attempt at looking up a short domain name" (Exercise 4, "Pick a transport", item 4) → `/networking/dns/`
- Anchor text "TLS 1.3 folded in" (in "Choosing between TCP, UDP and building on top of UDP" section) → `/networking/tls-and-https/`
- Anchor text "HTTP/3" (same section, last sentence) → `/networking/http-explained/` (once that article covers HTTP/1.1 vs 2 vs 3, per its planned outline)
