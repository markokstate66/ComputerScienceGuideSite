# Wanted cross-links: /version-control/branching-and-merging/

One line each: anchor text | target route | where in the article.

- "`--no-ff`" / "Which of the two a team prefers is policy" | /version-control/git-workflows/ | section "Fast-forward: the merge that creates nothing", paragraph after the option list — Status: wired (body, linked "Which of the two a team prefers is policy" rather than "--no-ff", since that phrase is exactly the paragraph after the option list the request points to)
- "`--ff-only`" (keeping a branch free of local merge commits) | /version-control/rebase-vs-merge/ | same section, option list — Status: wired (body, option list)
- "`--squash`" (a merge that records no merge) | /version-control/rebase-vs-merge/ | same section, paragraph after "Which of the two a team prefers" — Status: dropped (duplicate target — already linked earlier in this section via "--ff-only"; site policy is first occurrence only)
- "`git reset --hard ORIG_HEAD`" | /version-control/undoing-things-in-git/ | section "After the merge: deleting branches, undoing merges", paragraph "Undoing a merge depends on whether anyone else has it" — Status: wired (body, that exact paragraph; the earlier exercise-solution mention of the same command was left unlinked as this note asked)
- "`HEAD`'s reflog" | /version-control/undoing-things-in-git/ | same section, paragraph about `-D` (wire at most these two links to undoing-things-in-git; skip the exercise solution) — Status: wired (body, the `-D` paragraph; wired alongside the ORIG_HEAD link above per this note's explicit two-link allowance)
