# Crosslinks for testing/testing-pyramid-and-integration-tests

Targets that do not exist yet in the build. Wire these in once the target is published.

- Anchor text: "AAA" or "Fact/Theory basics" — target `/testing/unit-testing-fundamentals/` — where: the opening paragraph of "What actually distinguishes a unit test from an integration test?", which currently defines unit/integration/E2E without linking back to the pillar's entry point because it is unpublished. — Status: wired (no literal "AAA"/"Fact/Theory basics" text existed, so added a short clause at the end of the section's paragraph on unit-test isolation: "the AAA shape and Fact/Theory basics of writing one are unit testing fundamentals' subject, assumed here"; note the article's front matter already lists `testing/unit-testing-fundamentals` as a prerequisite, which surfaces separately as "Before you read")
- Anchor text: "red, green, refactor" or "TDD" — target `/testing/test-driven-development/` — where: not currently referenced in prose, but if a future revision adds a sentence connecting integration testing to a TDD workflow, link there. — Status: dropped (still no sentence connecting integration testing to a TDD workflow; the request itself frames this as a future/speculative addition, and inventing one now would be filler)
- Anchor text: "property-based testing" — target `/testing/property-based-testing/` — where: could be mentioned as a complementary technique for generating the edge-case inputs used in the "Predict the match" exercise, once published. — Status: dropped (that exercise tests one fixed pair of values, `'CS101'` vs `'cs101'`, not generated edge cases; no natural sentence introduces property-based testing there, and the request marks this speculative)
- Glossary term "Flaky test" does not exist yet in `src/content/glossary/`. The article currently uses the unlinked term *flaky test* (italicized, no link) in "What makes integration tests flaky, and how do you stop it?". Once a `flaky-test.yaml` glossary entry exists, link it there. — Status: n/a (not an article link; no `flaky-test.yaml` glossary entry exists yet, and glossary entries are outside this pillar's scope)
