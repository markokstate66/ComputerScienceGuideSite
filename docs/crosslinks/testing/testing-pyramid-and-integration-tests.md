# Crosslinks for testing/testing-pyramid-and-integration-tests

Targets that do not exist yet in the build. Wire these in once the target is published.

- Anchor text: "AAA" or "Fact/Theory basics" — target `/testing/unit-testing-fundamentals/` — where: the opening paragraph of "What actually distinguishes a unit test from an integration test?", which currently defines unit/integration/E2E without linking back to the pillar's entry point because it is unpublished.
- Anchor text: "red, green, refactor" or "TDD" — target `/testing/test-driven-development/` — where: not currently referenced in prose, but if a future revision adds a sentence connecting integration testing to a TDD workflow, link there.
- Anchor text: "property-based testing" — target `/testing/property-based-testing/` — where: could be mentioned as a complementary technique for generating the edge-case inputs used in the "Predict the match" exercise, once published.
- Glossary term "Flaky test" does not exist yet in `src/content/glossary/`. The article currently uses the unlinked term *flaky test* (italicized, no link) in "What makes integration tests flaky, and how do you stop it?". Once a `flaky-test.yaml` glossary entry exists, link it there.
