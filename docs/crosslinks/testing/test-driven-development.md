# Cross-links wanted for testing/test-driven-development

- Prerequisite `testing/unit-testing-fundamentals` is already declared in front matter `prerequisites`; per `docs/WRITER_GUIDE.md` §2 it will surface automatically as "Before you read" once that article publishes, so no manual body link is needed for it. The intro section ("The loop, and what each third of it is for") deliberately does not re-explain AAA, Fact/Theory or test naming, since that article covers them; once it is live this article could gain one short sentence pointing at it explicitly (e.g. after "each new test below follows the arrange-act-assert shape"), which is optional polish, not a missing link. — Status: n/a (not a link request — the "Before you read" prerequisite mechanism already covers it, no sentence containing "arrange-act-assert shape" exists in the article to attach a link to, and the note itself flags any addition as optional polish; adding one would be filler)
- No other planned-but-unpublished articles are referenced. The one link to a published sibling (`/testing/test-doubles/`, in the "Refactor" section: "`Game` never needed a test double") already resolves, since that article is live. — Status: n/a (informational note, confirms an existing link already resolves; nothing to wire)
