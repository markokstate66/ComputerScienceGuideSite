# Cross-links for algorithms/recursion

- Anchor text: "Analyzing the Complexity of Loops and Recursion" (currently plain italic text, not a link) — Target route: `/complexity/analyzing-loops-and-recursion/` — Where: the paragraph right after the Fibonacci recursion-tree code/output in "The same subproblem, asked twice: Fibonacci's tree", explaining that this article covers the shape of recursive branching but not its growth rate or the recurrence-based analysis (recursion trees, Master theorem), which that article owns. — Status: wired (body, "The same subproblem, asked twice: Fibonacci's tree")
