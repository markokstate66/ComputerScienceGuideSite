# Cross-links wanted by /complexity/space-complexity/

One line each: anchor text | target route | where in the article. All targets are planned in `CONTENT_PLAN.md` section 7 and were unpublished when this article was written, so the body does not link them yet. Each sentence reads correctly with or without the link.

- "best, average and worst case" (or "Best, Average and Worst Case") | /complexity/best-average-worst-case/ | "In place doesn't mean free", the paragraph after the quicksort-depth measurement, sentence beginning `"Small auxiliary stack" is a claim about *typical* partitions, not a guarantee; a later article in this pillar on best, average and worst case covers what forces the bad case and how randomization avoids it.` Currently plain text with no link.
- "dynamic programming" | /algorithms/dynamic-programming/ | "Trading space for time: memoizing Fibonacci", closing paragraph, sentence beginning `Whether that price is worth it, and how to shrink it once you have paid it, is the subject of dynamic programming, covered in its own article in the algorithms pillar`. Currently plain text with no link.
- "recursion" | /algorithms/recursion/ | "The call stack spends memory too", opening sentence (`ReverseInPlace` above has no [recursion]...). Currently links to the glossary entry `/glossary/#recursion`; switch to the article once published, since that page owns the fuller treatment of base/recursive cases and the call-stack trace this page assumes.
- "sorting algorithms compared" (or "how the other classic sorts compare") | /algorithms/sorting-algorithms-compared/ | "In place doesn't mean free", opening paragraph, as an aside after the quicksort-vs-merge-sort space measurement, for a reader who wants the full sort lineup and time comparison rather than just the space angle this page takes.

## Boundaries with planned siblings (to avoid near-duplicate coverage)

- `/complexity/best-average-worst-case/` owns the full definition of best/average/worst case, the quicksort-dies-on-sorted-input case study, and randomization as the fix. This article uses one measurement (recursion depth 26 vs. 4,999) only to show that quicksort's call-stack space depends on the same partition luck, and does not re-derive why sorted input is adversarial.
- `/algorithms/dynamic-programming/` owns the full memoization-to-tabulation-to-space-optimization arc (coin change, LCS, edit distance, space-optimized DP tables). This article uses only naive-vs-memoized-vs-iterative Fibonacci, measured for time *and* space, to make one narrower point: memoization is a real space-for-time trade, not a free win.
- `/algorithms/recursion/` owns base/recursive cases, the call-stack trace, recursion vs. iteration, and tail calls. This article assumes the reader already has that model and measures the space side of it directly (frame size, depth ceilings), rather than teaching recursion from scratch.
- `/algorithms/sorting-algorithms-compared/` owns the full sort lineup, stability, and the Ω(n log n) comparison-sort lower bound, with a time-focused decision table. This article's quicksort/merge-sort code measures only auxiliary space (bytes allocated, recursion depth), never timing.
