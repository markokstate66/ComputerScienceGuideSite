# Cross-links wanted by /complexity/analyzing-loops-and-recursion/

One line each: anchor text | target route | where in the article. All targets are planned in `CONTENT_PLAN.md` section 7 and are unpublished (`draft: true` or not yet drafted) as of 2026-09-22, so the body does not link them yet. Each sentence reads correctly with or without the link.

- "recursive" (first use) | /algorithms/recursion/ | "How do you find the complexity of a recursive function?", first sentence ("the shape behind every recursive function below"). Currently links to the glossary entry /glossary/#recursion; switch to the article once published (it is the dedicated treatment of base/recursive case, the call stack, and tail calls — this page only borrows the vocabulary). — Status: wired (switched from glossary to article)
- "space complexity" | /complexity/space-complexity/ | The `:::dotnet` callout in the Master Theorem section, sentence "That is a space complexity concern, not a time one." Currently links to the glossary entry /glossary/#space-complexity; switch to the article (its planned outline explicitly covers "the call stack as space"). — Status: wired (switched from glossary to article)
- "default thread stack size... StackOverflowException" (the whole `:::dotnet` callout) | /algorithms/recursion/ | Same callout. That article's planned outline covers "stack overflow and depth limits in .NET" and "tail calls (what the CLR does and does not guarantee)" directly; this page's callout is a brief aside, not the full treatment, and should point there once it exists instead of standing alone. — Status: dropped (/algorithms/recursion/ is already linked once in this article via the "recursive" request above; the integration rule forbids linking the same target twice in one article, so the first natural occurrence stands and this second link was not added)
- "Quicksort as a Case Study" (or similar anchor on "quicksort's worst case") | /complexity/best-average-worst-case/ | "When does the Master Theorem not apply?", the quicksort paragraph ("Quicksort's worst case doesn't divide the input at all"). That article's planned lead example is exactly this scenario — quicksort dying on sorted input — and owns the full three-case treatment (worst/average/expected via randomization); this page only needs the one recurrence. — Status: wired ("Quicksort's worst case" linked in the bold lead sentence)
- "sorting algorithms" (or "the sorting toolbox") | /algorithms/sorting-algorithms-compared/ | Could be added near the Case 2 (merge sort) subsection if a natural sentence introduces it; that article owns the comparison sort survey and the Ω(n log n) lower bound. Optional — no sentence currently requires it. — Status: dropped (still no natural sentence introduces sorting algorithms near Case 2; author flagged this optional, and inventing a clause would be filler, so left unlinked)

## Boundaries with planned siblings (to avoid near-duplicate coverage)

- /algorithms/recursion/ owns recursion as a topic in its own right: base/recursive case terminology, the call-stack trace, recursion vs. iteration, stack overflow and depth limits, and tail calls. This article assumes the reader already knows what a recursive function is (via the prerequisite chain) and only analyzes the *cost* of recursive code — recursion trees, recurrences, the Master Theorem. It does not re-teach what recursion is.
- /complexity/best-average-worst-case/ owns the formal definitions of best/average/worst case and the full quicksort case study (naive vs. randomized pivot, adversarial inputs, hash flooding). This page's quicksort mention is a single paragraph illustrating *why* its worst-case recurrence falls outside the Master Theorem's shape, not an analysis of quicksort itself.
- /complexity/space-complexity/ owns auxiliary vs. total space, the call stack as space, and time-space trades. This page's `:::dotnet` callout only notes that a Θ(n)-deep recursion allocates Θ(n) stack frames, as a caveat on the time-complexity results above it.
- /algorithms/sorting-algorithms-compared/ owns the full sort comparison and the Ω(n log n) lower bound for comparison sorts. This page derives only the Θ(n log n) recurrence for one already-named algorithm (merge sort), continuing the crossover measurement /complexity/big-o-notation/ made without proving it.
