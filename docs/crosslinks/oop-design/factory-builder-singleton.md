# Cross-links wanted by /oop-design/factory-builder-singleton/

One line each: anchor text | target route | where in the article. `/oop-design/dependency-injection/` (CONTENT_PLAN.md §7.4, row 7) is planned but was being written concurrently by another writer this round and was unpublished when this article was drafted (2026-09-22), so the body does not link it yet. Each sentence reads correctly with or without the link.

- "constructor injection" | /oop-design/dependency-injection/ | "The DI lifetime that replaced it", last paragraph, sentence "constructor injection gives every caller an explicit, substitutable dependency". That article's own outline covers constructor injection from first principles (hard-coded dependency -> constructor injection by hand -> composition root); this article only uses the technique to fix one testability problem, not to explain it. — Status: dropped (same target already wired at its first occurrence in this article, "the DI container" below; the anti-duplicate-link rule allows only the first natural occurrence per target per article)
- "the DI container" (first use) | /oop-design/dependency-injection/ | "The DI lifetime that replaced it", opening sentence "Microsoft's own guidance for the DI container is direct about the fix". That article owns the container itself (building a small one by hand, then `Microsoft.Extensions.DependencyInjection`); this article only uses `ServiceCollection`/`AddSingleton` as a finished tool. — Status: wired (that opening sentence, "the DI container" linked)
- "composition root" | /oop-design/dependency-injection/ | Would fit in "The DI lifetime that replaced it", if a sentence is added explaining *where* `services.AddSingleton<IEmailSender, EmailSender>()` is normally called from in a real app (this article's `di-singleton-lifetime` example calls it inline for the demo, not from a composition root). Optional — no sentence currently requires it, but the concept would strengthen the "one configuration choice" framing. — Status: dropped (optional; no existing sentence to attach the link to, and adding one would be new content beyond this wiring pass)

## Boundaries with the planned sibling

- `/oop-design/dependency-injection/` owns dependency injection itself: the hard-coded-dependency problem in general, constructor injection built by hand, the composition root, all three container lifetimes (not just Singleton), a hand-rolled ~60-line container, `Microsoft.Extensions.DependencyInjection`, and the captive-dependency bug (a scoped service injected into a singleton).
- This article (`factory-builder-singleton`) only reaches into that territory far enough to make its Singleton critique land: it shows *one* lifetime (`Singleton`) as the fix for *one* concrete testability problem (a static-instance email sender), via constructor injection it does not otherwise explain. It should not be extended to cover Transient/Scoped, the composition root pattern in depth, or the captive-dependency bug — those stay owned by the DI article.
- If `/oop-design/dependency-injection/` publishes a captive-dependency example before this article is finalized, consider a short forward-mention near the `Singleton services must be thread safe` sentence in "The DI lifetime that replaced it" (a singleton depending on something with a shorter lifetime is a related but distinct mistake this article does not demonstrate).
