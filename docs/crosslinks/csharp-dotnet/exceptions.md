# Cross-links wanted by `/csharp-dotnet/exceptions/`

No links to planned-but-unpublished articles were identified for this round. The three siblings this article builds on — `garbage-collection` (`using`/`IDisposable`), `async-await` (how an awaited method's exception surfaces), and `recursion` (the call stack an exception climbs) — are all published already and are linked directly from the body.

If a future round finds a natural forward link (for example once `csharp-dotnet/span-and-memory` or `databases/sql-injection-and-parameters` exist), add it here first rather than linking straight into the body, since neither is published as of this round (2026-09-24).
