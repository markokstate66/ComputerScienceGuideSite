# Crosslink requests: databases/sql-injection-and-parameters

Link requests to articles that are planned but not yet published. The integrator wires these in wave 3.

- Anchor text: "how `throw;` and `throw ex;` differ when a caught `SqliteException` is rethrown" — target: `/csharp-dotnet/exceptions/` — where: "Least privilege limits the damage, not the injection" section, near the `catch (SqliteException ex)` block, as a soft follow-up on handling the exception a rejected write throws (module A-14's soft edge to A-11 in `docs/ARTICLES_PLAN.md` §4).
