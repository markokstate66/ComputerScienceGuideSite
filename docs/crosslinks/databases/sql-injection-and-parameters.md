# Crosslink requests: databases/sql-injection-and-parameters

Link requests to articles that are planned but not yet published. The integrator wires these in wave 3.

- Anchor text: "how `throw;` and `throw ex;` differ when a caught `SqliteException` is rethrown" — target: `/csharp-dotnet/exceptions/` — where: "Least privilege limits the damage, not the injection" section, near the `catch (SqliteException ex)` block, as a soft follow-up on handling the exception a rejected write throws (module A-14's soft edge to A-11 in `docs/ARTICLES_PLAN.md` §4). — Status: wired. The recorded text didn't fit verbatim: this catch only inspects `ex.SqliteErrorCode` and never rethrows, so a sentence flatly describing "a caught SqliteException is rethrown" would misstate the code. Added a smallest-natural-clause parenthetical right after the error-8 explanation instead: "(The catch above only inspects `ex.SqliteErrorCode`; see [how `throw;` and `throw ex;` differ](/csharp-dotnet/exceptions/) when a caught `SqliteException` is rethrown instead.)" — accurate about what the code does and still links the requested target with the requested anchor phrase.
