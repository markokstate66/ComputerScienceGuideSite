{
  "article": "src/content/articles/version-control/undoing-things-in-git.md",
  "route": "/version-control/undoing-things-in-git/",
  "round": 2,
  "role": "technical",
  "score": 9.2,
  "runCodePass": "true: `node tools/run-code.mjs src/content/articles/version-control/undoing-things-in-git.md` from PowerShell printed `PASS version-control/undoing-things-in-git.md  blocks=55 executed=28`, exit 0, on 2 independent runs (git 2.52.0.windows.1), identical block/exec counts to round 1. The round-2 diff (`git diff ba8a29b..900f378 -- src/content/articles/version-control/undoing-things-in-git.md`) touches only 4 insertions / 2 deletions in prose, no code blocks, so the run-code determinism already proven exhaustively in round 1 (literal, non-wildcarded commit hashes reproducing byte-for-byte, including outside the harness's injected env) is untouched and still holds.",
  "comparedAgainst": [
    "https://git-scm.com/book/en/v2/Git-Basics-Undoing-Things",
    "https://ohshitgit.com/",
    "https://www.atlassian.com/git/tutorials/undoing-changes"
  ],
  "justification": "Scope for this round was the three round-1 findings only (AdSense and design are out of scope, both passed round 1 at 9.0). I confirmed via `git diff ba8a29b..900f378 -- src/content/articles/version-control/undoing-things-in-git.md` that the writer's round-2 commit changed exactly 4 lines added / 2 removed, in exactly the three locations the round-1 review flagged, and touched nothing else in the article -- the --soft/--mixed/--hard explanations, the alice/bob bare-remote scenario, and the reflog recovery mechanics (all independently verified accurate in round 1) are byte-identical to round 1. (1) The major: round 1's blocking issue was the --amend exercise solution's false universal claim that --amend works 'exactly like every other command on this page,' which is false because git restore never writes a commit or moves the branch, and every git reset mode moves the branch without ever writing a new commit object. The round-2 fix rescopes the sentence to a specific three-way comparison: 'like revert above, --amend writes a brand-new commit object and moves the branch to it -- the two commands on this page that undo something by creating a new commit, rather than by repointing a branch (reset) or touching only the working tree and index (restore).' I re-derived this from scratch rather than trusting the fix: restore, per the article's own quoted git-restore manual text, 'does not update your branch' and never writes a commit object -- confirmed correctly excluded. reset (soft/mixed/hard) 'relabels which commit is the branch tip' in every mode per the article's own text and the live git-reset manual, moving the branch but never writing a new commit object -- confirmed correctly excluded, and correctly described as 'repointing a branch' rather than the working-tree/index framing given to restore. revert 'record[s] some new commits' (article's own quote of git-revert) and that new commit becomes the branch tip -- both write-a-commit and move-the-branch, confirmed included. amend likewise both writes a new commit object (a different hash, since a commit's ID is a hash of its own content and nothing is edited in place) and moves the branch to it -- confirmed included. I also checked the one place this taxonomy could have a hidden gap: the reflog-recovery mechanism later on the page (`git branch sort-experiment HEAD@{1}`) creates a brand-new branch name pointing at an already-existing commit object -- it writes no new commit and does not move an existing branch, so it falls outside the two-way create-and-move category the fix claims is exhaustive, without contradicting it (the fix's comparison set is explicitly restore/reset/revert/amend, not reflog recovery, which the page treats as a lookup-and-repoint mechanism rather than one of the four undo verbs named in the title). The rescoped claim is genuinely true and no longer self-contradicts the article's own earlier, correct description of restore. (2) The connecting insight -- 'The original becomes unreachable from any branch, but the reflog records the move, the same way it recorded the branch deletion above' -- is preserved verbatim immediately after the edited clause and still reads correctly: it is orthogonal to the create/move taxonomy and was never part of what round 1 flagged. (3) The 'entire reason to prefer revert' overstatement is now 'one real reason to prefer revert -- alongside protected-branch policies that block force-pushes outright and the audit trail a revert leaves that a rewritten history does not.' Both added reasons are genuine: branch-protection settings that reject force-pushes to a given branch are a standard, real mechanism (GitHub/GitLab/Azure DevOps all expose exactly this), independent of whether anyone has already fetched the commit, so it is a distinct reason from the fast-forward/rewrite asymmetry the section just demonstrated; and the audit-trail point is not new material invented for the fix, it is the article's own two-sentences-earlier observation ('Add task: call the vet is still right there in the log, unedited') now correctly credited as a second reason rather than left implicit. (4) The new sentence explaining why GIT_AUTHOR_DATE/GIT_COMMITTER_DATE is not re-exported for alice and bob -- 'The git config lines are repeated because each git init starts a fresh .git/config, local to that repository. GIT_AUTHOR_DATE and GIT_COMMITTER_DATE, exported once for the planner repo near the top of this page, are shell environment variables, not repository config, so they carry forward for the rest of the session and apply here too' -- is technically accurate and placed correctly: I confirmed by line search that the two `export` statements appear exactly once, in the planner repo's setup block, and this new sentence sits between the alice repo's `git config` block and the bob repo's `git init`, i.e. exactly where a reader would otherwise wonder why the pattern changed. The distinction it draws (per-repository git config in `.git/config`, reset by every `git init`, versus per-shell-session environment variables that `cd`/`git init` do not clear) is correct ordinary shell/git semantics, and matches the identical convention already established and explained in how-git-works.md that round 1 cited as this article's own stated model to follow. This closes round 1's minor finding without overclaiming anything the article doesn't already rely on. No new factual errors were introduced by any of the three edits, and nothing outside the three targeted locations changed. Round 1's exhaustive sourcing verification (all seven cited git-scm.com pages checked quote-for-quote, the alice/bob multi-clone demonstration reproduced independently outside the harness, the reflog-deletion and gc.reflogExpire mechanics confirmed) stands unmodified this round since none of that text was touched. The remaining 0.8 below a perfect score reflects round 1's own residual assessment that the article's strength is demonstration depth and sourcing precision rather than needing further improvement now that all three flagged issues are cleanly resolved -- I found nothing left to dock for.",
  "issues": []
}
