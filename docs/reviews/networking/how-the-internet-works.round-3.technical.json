{
  "article": "src/content/articles/networking/how-the-internet-works.md",
  "route": "/networking/how-the-internet-works/",
  "round": 3,
  "role": "technical",
  "score": 8.8,
  "comparedAgainst": [
    "https://developer.mozilla.org/en-US/docs/Web/Performance/Guides/How_browsers_work (fetched fresh this round: still says TLS 'requires five more round trips to the server before the request for content is actually sent', which is the TLS 1.2 story and wrong for the TLS 1.3 handshake this article measures; DNS/TCP/TLS covered in prose only, the only code block is a static HTML sample, no RFCs cited, no measurements)",
    "https://github.com/alex/what-happens-when (fetched fresh this round: TLS section describes ClientHello/ServerHello/client encrypts a random secret with the server's public key/Finished, i.e. the RSA key-exchange TLS 1.2 flow, not TLS 1.3; no RFC citations beyond Wikipedia-level links, no runnable code, no measured timings)"
  ],
  "justification": "No new factual error found this round, so the 7.0 cap does not apply, and run-code is green (24 blocks, 11 executed, PowerShell, exit 0). I formed a fresh read of the whole article before opening any prior review. I re-verified primary sources myself rather than trusting round 2's list: RFC 9110 4.2.1/4.2.2/4.3.4/5.1/7.1/15/1.2, RFC 9112 2.1/3/3.2/6.3/7.1/9.3, RFC 9293 3.1/3.4.1/3.5/3.5.2/3.7/3.7.4/3.8.6.3, RFC 791 1.4/3.1/3.2, RFC 1122 1.1.2/1.1.3, RFC 8446 1/1.2/2/2.3/5.1/5.2, RFC 6066 3, RFC 7301 1/3.1, RFC 5116 5.2, RFC 3022 2.2, RFC 6797 8.3 (fetched: it upgrades an existing http URL for a Known HSTS Host, it does not choose a scheme for a bare name -- the current wording 'the browser applies its own policy to choose a scheme... once it holds an http URL, one written rule can still change it' matches this exactly, confirming round 2's fix), and RFC 9113 9.1.1, the section newly cited this round for cross-origin HTTP/2 connection reuse (confirmed via search of the spec text plus corroborating secondary sources -- WebFetch could not render past section 6 of the very long RFC 9113 page, so I cross-checked with a targeted search that quoted the operative sentence: reuse is permitted when the server is authoritative and the certificate would pass the checks a fresh TLS connection to that host would require). All read as supporting the sentence that cites them. Arithmetic re-checked: 0x22f = 559, 5+559+7 = 571, 56+1+16+5 = 78, 78+20+20 = 118, 10 x 1460 = 14,600. I compared the round-2 and round-3 text with `git diff` line by line rather than re-deriving every measurement from scratch, since round 2 already re-ran the programs and matched the article's numbers; I only re-ran run-code myself (see runCodePass) and independently confirmed the two comparison sources and two of the newly touched citations. Against the two best-ranking pages I could find for this query (plus a quick check of bytebytego.com, which skips TLS and DNS internals entirely and defers to 'another post'), this article is still clearly ahead: it is the only one of the three with runnable, machine-checked C#, section-level RFC citations, and real measured timings, and it is the only one that gets the TLS 1.3 handshake right instead of describing the retired RSA key-exchange flow. I checked round 2's Recheck claims against the current text and confirm every round-2 minor was genuinely fixed, not papered over: the DNS-cold-lookup wording now says 'at least a round trip ... and more if that server does not have the record either' (was an unscoped 'roughly'); the TLS/HTTP-cost paragraph now says 'between one and two of those round trips' instead of 'roughly one round trip'; the certificate-refusal sentence now allows for a default-certificate rejection as an alternative to a server alert; the NoDelay-exercise sentence is now hedged with 'If ...' instead of asserted; the HSTS paragraph is reworded as verified above; and the same-host/new-host paragraph now scopes parallel connections and cites RFC 9113 9.1.1 for HTTP/2 connection reuse across names. What I found instead is one fresh, self-inflicted defect introduced by this round's own editing pass (visible in `git diff`): the sentence 'Header names are case-insensitive ([RFC 9110, section 5.1]), which is why `IsFraming` ignores case.' was pasted twice, verbatim, back to back, at the end of the 'Where the response ends' paragraph. It is not a factual error -- the sentence is true both times -- but it is literal, avoidable padding of exactly the kind Rule 6 forbids, introduced in the same revision that fixed seven other issues, which is a real quality regression a competent copyedit pass should have caught. That keeps this out of the 9-10 band on its own merits (not '10 minus effort'): a reader hits a jarring, obviously mechanical repetition in the middle of otherwise the strongest prose on the page. Two lingering, very small nits: (1) the ping program's `new byte[32]` payload is still unexplained (round 2 offered dropping `dontFragment` as an alternative fix and the writer took that path, but the payload-size mystery this issue also named is still there); (2) the loopback capture's `seen += Encoding.ASCII.GetString(...)` now concatenates a string in a loop where a `StringBuilder` would be the idiom for anything larger, though for a request under 1 KB read in one or two iterations this has no practical cost and is not the same bug round 2 flagged (culture-sensitive comparison), which is genuinely fixed. Neither of these caps the score; they are the kind of nit that separates 8.8 from a flawless 10.",
  "issues": [
    {
      "severity": "major",
      "location": "\"Where the response ends\" section, end of the paragraph after the `Content-Length` sentence: \"Header names are case-insensitive ([RFC 9110, section 5.1]...), which is why `IsFraming` ignores case.\"",
      "problem": "The sentence is duplicated verbatim, back to back, in the current text (confirmed with `git diff` against the round-2 version, where it appears once as a genuine, correct fix and is then immediately repeated). This is literal filler introduced during this round's revision pass, not left over from an earlier round: it violates the 'No filler' hard rule and reads as a mechanical copy-paste error to any reader, undercutting the credibility this article otherwise earns through careful, exact prose.",
      "fixLooksLike": "Delete the second occurrence so the sentence appears once.",
      "owner": "writer"
    },
    {
      "severity": "minor",
      "location": "Program id=hops: `PingReply reply = await ping.SendPingAsync(found[0], wait, new byte[32], options);`",
      "problem": "Carried over from round 2: the 32-byte ping payload is unexplained. Round 2 offered two independent fixes ('drop dontFragment, or add a half sentence saying they mirror tracert's defaults'); the writer dropped `dontFragment` but the payload size is still an unexplained magic number a curious reader will ask about.",
      "fixLooksLike": "Either use `[]` / `Array.Empty<byte>()` if the payload content doesn't matter, or add a half-sentence noting 32 bytes mirrors the Windows `ping`/`tracert` default payload size.",
      "owner": "writer"
    },
    {
      "severity": "minor",
      "location": "\"The request a real client writes\", loopback server: `string seen = \"\"; ... seen += Encoding.ASCII.GetString(buffer, 0, n);`",
      "problem": "Round 2 flagged this loop for a culture-sensitive `EndsWith` and a `StringBuilder`-via-interpolation allocation habit; the culture bug is genuinely fixed (`StringComparison.Ordinal` now used), but the rewrite replaced `StringBuilder.Append` with `+=` string concatenation inside a `while` loop, which is the pattern the C# idiom guidance in Rule 5 (modern, idiomatic C#) usually asks readers to avoid, even though for a request under 1 KB read in one or two iterations here it has no measurable cost.",
      "fixLooksLike": "Use `var seen = new StringBuilder();` with `seen.Append(...)` and `seen.ToString()` at the end, as in the pre-round-2 version, but keep the `StringComparison.Ordinal` fix on `EndsWith`.",
      "owner": "writer"
    }
  ],
  "runCodePass": "PASS networking/how-the-internet-works.md  blocks=24 executed=11 (exit 0), run from PowerShell on 2026-09-21."
}
