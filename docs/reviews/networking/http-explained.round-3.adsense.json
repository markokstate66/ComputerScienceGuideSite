{
  "article": "src/content/articles/networking/http-explained.md",
  "route": "/networking/http-explained/",
  "round": 3,
  "reviewer": "adsense",
  "targetQuery": "http protocol explained (methods, status codes, caching, cookies)",
  "score": 9.2,
  "comparedAgainst": [
    "https://www.rfc-editor.org/rfc/rfc9110.txt (plaintext edition, fetched directly via curl to avoid the WebFetch summarizer's known truncation problem with this document) — re-verified section 9.2.2 in full: 'A request method is considered \"idempotent\" if the intended effect on the server of multiple identical requests with that method is the same as the effect for a single such request. Of the request methods defined by this specification, PUT, DELETE, and safe request methods are idempotent.' Confirms 9.2.2 is a general list naming PUT and DELETE idempotent, matching the article's new clause 'which also names PUT and DELETE as idempotent'.",
    "RFC 9110 section 9.3.4 (PUT), same plaintext fetch, lines 4074-4196 — contains the sentence 'Hence, the intent of PUT is idempotent and visible to intermediaries, even though the exact effect is only known by the origin server.' The article's quotation 'the intent of PUT is idempotent' is a verbatim substring, correctly attributed.",
    "RFC 9110 section 9.3.5 (DELETE), same plaintext fetch, lines 4197-4256 — read in full and additionally grepped case-insensitively for 'idempotent': zero matches (grep exit code 1, no output). Confirms the article's new claim that DELETE's own section 'never uses the word' is accurate.",
    "git diff bc31a5f..e09914e (round-2 writer commit to round-3 writer commit) on the article file — confirms the entire round-3 change is exactly one paragraph, 1 file changed / 1 insertion / 1 deletion, touching only the idempotency-citation sentence; nothing else in the article was altered.",
    "Spot-checked every other direct RFC quotation still in the article against freshly re-fetched plaintext RFCs (9110, 9111, 9112, 5789), to rule out a second latent misattribution: RFC 9110 §1.1 ('a family of stateless, application-level, request/response protocols') — verbatim, correct section. RFC 9112 §9.3.2 ('MUST send the corresponding responses in the same order that the requests were received') — verbatim, correct section. RFC 5789 §2 ('is neither safe nor idempotent') — verbatim substring of 'PATCH is neither safe nor idempotent as defined by [RFC2616], Section 9.1', correct section (the sentence sits directly under the '2. The PATCH Method' heading, not a subsection). RFC 9111 §5.2.2.5 (no-store, 'must not store any part of either the immediate request or the response') — verbatim substring of 'a cache MUST NOT store any part of either the immediate request or the response...', correct section; only the RFC's normative capitalization (MUST NOT) is lowered in the article's quote, a cosmetic de-shouting also visible in round 2's already-passed ETag/If-None-Match quotes' surrounding prose style — not a misattribution and not scored down. The ETag (§8.8.3) and If-None-Match (RFC 9111 §4.3.1) quotations were independently re-verified verbatim in round 2 and are untouched by the round-3 diff, so were not re-fetched again here.",
    "node tools/run-code.mjs src/content/articles/networking/http-explained.md, run from PowerShell — PASS networking/http-explained.md blocks=16 executed=6, matching the writer's claim exactly."
  ],
  "justification": "The single defect that held round 2 at 7.8 is genuinely and completely fixed. Round 2's one open finding was that the sentence 'PUT and DELETE add nothing new but are idempotent anyway, each defined that way in its own section ([9.3.4], [9.3.5])' misattributed DELETE's idempotency to section 9.3.5, which never uses the word 'idempotent'. The round-3 rewrite reads: 'Safe methods ... are idempotent by construction ([RFC 9110, section 9.2.2]), which also names PUT and DELETE as idempotent; PUT adds nothing new and restates that in its own section (\"the intent of PUT is idempotent,\" [9.3.4]), while DELETE's idempotency rests only on that general list — its own section ([9.3.5]) never uses the word.' I re-fetched RFC 9110 directly as plaintext (bypassing the WebFetch summarizer per the brief's warning, since a prior round hit unreliable truncation with this exact document) and confirmed all three claims independently: (1) section 9.2.2 states 'Of the request methods defined by this specification, PUT, DELETE, and safe request methods are idempotent' — a general list, exactly as the article now says; (2) section 9.3.4 contains the verbatim sentence fragment 'the intent of PUT is idempotent', so PUT's own section restating idempotency is accurate; (3) section 9.3.5 (DELETE), read in full and grepped case-insensitively for 'idempotent', contains zero occurrences — confirming the article's explicit claim that DELETE's own section never uses the word. Every clause of the rewritten sentence is now independently verifiable and accurate; nothing is overstated or newly fabricated in the fix.\n\nThe fix is also complete rather than partial: git diff between the round-2 and round-3 writer commits (bc31a5f..e09914e) touches exactly one file with one insertion and one deletion — this single paragraph and nothing else. That matches the brief's expectation of a surgical, single-sentence citation fix with no collateral changes to the article's structure, exercises, sources, or other prose.\n\nBecause this is the third round and citation precision was the whole story, I did not stop at re-verifying only the fixed sentence. I re-fetched RFC 9110, 9111, 9112 and 5789 as plaintext and checked every other direct quotation still present in the article against its cited section: the idempotent-method definition (9.2.2), HTTP's statelessness (9110 §1.1), pipelining's response-ordering MUST (9112 §9.3.2), PATCH being neither safe nor idempotent (5789 §2), and Cache-Control: no-store (9111 §5.2.2.5) all match their cited sections verbatim (the no-store quote lowers the RFC's normative MUST NOT to lowercase, a cosmetic style choice consistent with how the article treats RFC keywords elsewhere, not a misattribution). The ETag (9110 §8.8.3) and If-None-Match (9111 §4.3.1) quotations, the subject of round 1's major finding, were independently re-verified verbatim in round 2 and are unchanged by this diff, so I relied on that prior verification rather than re-fetching them again. No second latent misattribution was found anywhere in the article.\n\nnode tools/run-code.mjs reports PASS networking/http-explained.md blocks=16 executed=6, matching the expected figures exactly and confirming no code sample regressed. Technical (8.8) and design (9.3) already passed round 2 and nothing in this diff touches code, structure, or the diagrams those reviews covered, so there is no reason to expect either would move. Originality and the dedup boundary with how-the-internet-works.md were already confirmed clean in round 1/2 and this round adds no new demo code or claims that could reopen that question.\n\nWith the last open issue resolved cleanly, verified independently against primary sources, and confirmed as the only change in the diff, this round clears the 8.5 AdSense bar comfortably. Score reflects a fully accurate, well-sourced, non-filler article whose every direct quotation I checked traces to the section it cites.",
  "issues": []
}
