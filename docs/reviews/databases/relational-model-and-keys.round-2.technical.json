{
  "article": "src/content/articles/databases/relational-model-and-keys.md",
  "route": "/databases/relational-model-and-keys/",
  "round": 2,
  "role": "technical",
  "score": 8.8,
  "runCodePass": true,
  "runCodeResult": "PASS databases/relational-model-and-keys.md  blocks=72 executed=42 (run via `node tools/run-code.mjs src/content/articles/databases/relational-model-and-keys.md` from PowerShell on this machine; up from round 1's blocks=70 executed=41, matching the two new blocks the STRICT fix adds)",
  "comparedAgainst": [
    "https://www.ibm.com/think/topics/relational-databases",
    "https://www.freecodecamp.org/news/learn-relational-database-basics-key-concepts-for-beginners/"
  ],
  "justification": "Formed a fresh judgement of the whole article before opening the round-1 reviews. I independently re-verified the round-1 major and both round-1 minors, plus spot-checked claims elsewhere in the piece, rather than trusting that effort alone earns credit. Result: the round-1 major is genuinely fixed and nothing else regressed, and the score is uncapped accordingly. Specifics: (1) I built the exact `loan` DDL from the article (STRICT, with fee_min_zero/due_after_loan/back_after_loan) against node:sqlite 3.50.4 (the article's stated toolchain and the engine tools/run-code.mjs uses) and confirmed `INSERT ... late_fee = 3.50` now fails with 'cannot store REAL value in INTEGER column loan.late_fee' -- the exact message the article shows -- while `late_fee = 3.0` succeeds and is stored as the integer 3 (`typeof` = 'integer'), matching the article's new claim that a REAL literal with no fractional part is losslessly coerced. I also confirmed against sqlite.org/stricttables.html that STRICT's coercion rule ('the usual affinity rules, as PostgreSQL, MySQL, SQL Server, and Oracle all do'... 'if the value cannot be losslessly converted... an SQLITE_CONSTRAINT_DATATYPE error is raised') supports exactly this behaviour. (2) I checked that STRICT didn't silently break anything else in `loan`: every other value the page ever inserts or updates in that table (member_id/copy_id as whole numbers, loaned_on/due_on/returned_on as ISO-8601 text, late_fee as whole-number literals or the -5 CHECK-violation test) is already the declared type, and `run-code.mjs` passing end-to-end on the full page confirms this rather than just the local schema. (3) The pitfall-box portability rewording is now correctly scoped: it says 'the loose and tight tables just shown' work unchanged elsewhere and explicitly flags that STRICT is SQLite-only syntax with no keyword equivalent in PostgreSQL/SQL Server, closing the round-1 minor about a skimming reader over-generalising the portability claim to the whole schema. (4) The new Codd 1NF citation is accurate: I extracted the actual paper text (pdftotext on the University of Pennsylvania-hosted PDF the front matter cites) and found the article's quotes -- 'simple domains,' 'domains whose elements are atomic (nondecomposable) values,' and 'nonsimple domain' -- verbatim, and confirmed by section-heading position in the extracted text that this passage falls inside section 1.3 ('A RELATIONAL VIEW OF DATA', which runs from the 'in its accepted mathematical sense' definition up to the 1.4 NORMAL FORM heading), exactly as cited. This also resolves the AdSense round-1 minor about that sentence lacking an inline citation. Beyond the round-1 fix list, I re-checked a sample of older claims for a genuinely fresh pass: the SQL-standard PRIMARY-KEY-implies-NOT-NULL bug wording against sqlite.org/lang_createtable.html matches verbatim ('due to a bug in some early versions... SQLite allows NULL values in a PRIMARY KEY column'), the CHECK-forbids-subqueries claim matches verbatim ('The expression of a CHECK constraint may not contain a subquery'), and the ALTER-TABLE-cannot-retrofit-a-constraint-onto-an-existing-column claim in the dates exercise is accurate in its context (SQLite's ALTER TABLE only supports rename table/column, add column and drop column; a CHECK can only ride along with a brand-new ADD COLUMN, never attach to an already-declared column, which is exactly the case the article is in). Against the field for 'relational database explained', I read two of the pages that currently rank myself: IBM's overview names primary/foreign keys and ACID but has no candidate keys, no NULL-in-key discussion, no junction tables, no CHECK constraints, no referential-integrity actions, and no runnable/verified SQL; freeCodeCamp's walkthrough has some real SQL (verified only via screenshots, not machine-checked) but likewise skips candidate keys, referential-integrity actions, junction tables and CHECK constraints entirely. This article's seven-rule schema, three diagrams, NULL-in-a-key engine-disagreement box, three-engine ON DELETE comparison, partial-unique-index solution to 'one open loan per copy', and the CHECK-cannot-count-rows section (correctly citing SQLite's and PostgreSQL's own restrictions) have no counterpart in either competitor. I found no new factual error and no citation that fails to support the sentence it is attached to. That puts it solidly above the 8.5 (MDN/Real-Python-with-nits) anchor; it is held just short of a 9+ only because nothing about this round's changes reaches the 'unambiguously exceeds every existing result, no notes at all' bar the 10 anchor implies -- the fix is correct but narrow, and the piece's remaining texture (a few load-bearing links still pointing at unpublished pillar siblings, expected per the crosslinks file) is unchanged from a round that was already assessed as content-and-sourcing excellent.",
  "issues": []
}
