{
  "article": "src/content/articles/algorithms/dynamic-programming.md",
  "route": "/algorithms/dynamic-programming/",
  "round": 2,
  "role": "design",
  "score": 9.4,
  "scores": {
    "structureFlow": 9.5,
    "diagramQuality": 9.4,
    "codeReadabilityFormatting": 9.5
  },
  "comparedAgainst": [
    "https://www.geeksforgeeks.org/dsa/dynamic-programming/",
    "https://en.wikipedia.org/wiki/Dynamic_programming"
  ],
  "lighthouse": {
    "performance": 99,
    "accessibility": 100,
    "best-practices": 100,
    "seo": 100
  },
  "consoleErrors": 0,
  "brokenLinks": 0,
  "verifyNotes": "Ran `node tools/verify-page.mjs --drafts /algorithms/dynamic-programming/` from PowerShell (full serial run, Lighthouse included, worktree C:\\Users\\marko\\AppData\\Local\\Temp\\claude\\csg-worktrees\\issue-108-dynamic-programming, which has its own node_modules). Result: PASS, words=2793, code=22, fig=2, lh(p/a/bp/seo)=99/100/100/100, mobileOverflowPx=0, consoleErrors=[], consoleWarnings=[], broken=[], placeholders=[], report.pass=true. 40 screenshot parts captured (10 desktop-light, 10 desktop-dark, 15 mobile-light... actually 10/10/15/15 per screenshotParts) plus a fold shot per theme/viewport. Checks performed against round 1's three findings and the required content-change redraw: (1) grepped the SVG source for Figure 2's accent rect — now `<rect x=\"200\" y=\"58\" width=\"32\" height=\"22\" rx=\"3\" class=\"d-box-accent\"/>`, column 5's slot exactly (columns run in 32px steps from x=40, so column 5 is x=200-232), with the adjacent plain rects re-tiled to x=40/width=160 (columns 0-4, with column 0's own accent rect redrawn on top at x=40/width=32) and x=232/width=64 (columns 6-7) — no gaps, no overlaps; the column-5 text also now carries `d-mono d-bold d-text-accent`, matching column 0's accent-background+accent-text pairing. Opened desktop-light-part05, desktop-dark-part05, mobile-light-part07 and mobile-dark-part07 and visually confirmed the accent-colored cell is the bold '1' the arrow points at and the caption names (`dp[3][5]`), in all four theme/viewport combinations — the major round-1 bug is fixed. (2) awk-scanned every fenced ```csharp block for lines over 80 columns (widened the margin past the old 85-column check on purpose): zero hits, versus the two 87-column lines round 1 found. `EditDistanceTable` now reads `int best = Math.Min(dp[i - 1, j - 1], Math.Min(dp[i - 1, j], dp[i, j - 1]));` split across two lines (`int best = Math.Min(` fits standalone under 40 visible columns; the continuation line reaches the nested `Math.Min(` within the visible window, versus round 1 where the whole three-way comparison was one 87-column line and the nested call wasn't visible at all before a scroll) followed by a one-line ternary — this is the writer applying round 1's `fixLooksLike` almost verbatim. Confirmed on mobile-light-part12 / the exercise solution's 'S' branch (edit-script) is similarly split (`int sub = ..., del = ..., ins = ...;` then a separate `best`/`ops[i,j]` assignment), also under 80 columns. (3) Figure 2's `<defs>`/`<marker>` block is now at lines 267-269, immediately after `<desc>` and before the first `<rect>` (line 273) — matches WRITER_GUIDE's template order, same as Figure 1's convention. (4) Re-grepped for blank lines inside every `<figure>...</figure>` block: none in either figure. (5) Figure 1 was fully redrawn for the amount-8-to-12 content change: recomputed the tree by hand from `MinCoins`'s own code (coins iterated `[1, 4, 5]`) — root 12 branches to 11/8/7 (subtracting 1/4/5), 11 branches to 10/7/6, 8 branches to 7/4/3, with all three occurrences of remaining amount 7 (direct from 12, from 11, from 8) correctly given `d-box-accent`/`d-bold` — every branch, coin order and highlight matches the code and the figcaption/desc's prose description exactly. Viewed on desktop-light/dark-part02/03 and mobile-light/dark-part03: legible at all four theme/viewport combinations, labels readable at 390px, caption below the drawing per the phone rules. No other regressions found: heading levels, exercise solution placement, output-panel widths and the LCS/edit-distance sections (unaffected by either fix) were spot-checked in the screenshots and read as in round 1.",
  "justification": "This round's scope was the three round-1 findings, the content-driven Figure 1 redraw, and a fresh live verify-page.mjs pass with Lighthouse — not a full re-audit of axes round 1 already scored well, which are carried forward except where the fixes bear on them directly.\n\nDiagram quality is where round 1 failed and where this round's work concentrated, and every issue is resolved. Figure 2's accent rect now sits exactly on column 5 (x=200, width=32), the cell the figcaption names (`dp[3][5]`), the curved arrow points at, and the bold digit renders — confirmed both by SVG-source arithmetic (column boundaries are x=40+32k) and by looking at the actual rendered screenshots in all four theme/viewport combinations, where the accent-colored box, the bold text, and the arrow's endpoint now all agree. This was the round's actual failure cause (major severity) and it is fixed cleanly, including the knock-on detail of also accenting column 5's text (`d-text-accent`) so the highlight reads consistently with column 0's existing accent pairing, and re-tiling the flanking plain rects without leaving a gap or an overlap at the old column-4 boundary. The `<defs>` marker-before-use ordering (minor) is fixed, matching Figure 1's and WRITER_GUIDE's convention. And Figure 1's full redraw for the amount-8-to-12 change is correct: I independently traced `MinCoins(12)`'s first two levels by hand from the article's own code and every child, every coin-order branch, and all three highlighted occurrences of remaining amount 7 (direct from 12 paying a 5, from 11 paying a 4, from 8 paying a 1 — exactly what the figcaption claims) match. The figure is legible at 390px in both themes. Diagram quality goes from the round's weakest axis to on par with the other two.\n\nCode/readability formatting: both round-1-flagged 87-column lines are fixed, and by a wide margin — a fresh scan found zero csharp lines over 80 columns (not just under the old 85 threshold). The `EditDistanceTable` fix follows round 1's suggested restructuring almost exactly (`int best = Math.Min(...)` on its own lines, then a one-line ternary using `best`), which means the operative three-way comparison is visible meaningfully earlier in the horizontal scroll on a 390px screen than before, and the edit-script exercise's mirrored 'S' branch got the same treatment for consistency. No new formatting issues found in this pass.\n\nStructure/flow is unaffected by any of the three fixes and by the amount change (the prose was already written around 12 in the draft this round reviews, and it reads as a single coherent worked example throughout, with the greedy counter-example, the 180-call measurement, and Figure 1's caption all internally consistent with amount 12). Carried forward from round 1's 9.5.\n\nThe live verify-page.mjs run adds real numbers the round-1 design review didn't have: Lighthouse 99/100/100/100 (all four categories comfortably over the 90 gate), zero console errors, zero broken links, zero placeholder markers, and mobileOverflowPx=0 confirming no page-level horizontal overflow at 390px width in either theme. Nothing else regressed.",
  "issues": []
}
