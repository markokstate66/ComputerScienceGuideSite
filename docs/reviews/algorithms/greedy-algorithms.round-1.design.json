{
  "article": "src/content/articles/algorithms/greedy-algorithms.md",
  "route": "/algorithms/greedy-algorithms/",
  "round": 1,
  "role": "design",
  "score": 9.4,
  "comparedAgainst": [
    "https://www.geeksforgeeks.org/dsa/greedy-algorithms/",
    "https://en.wikipedia.org/wiki/Huffman_coding",
    "https://algs4.cs.princeton.edu/24pq/"
  ],
  "lighthouse": { "performance": 100, "accessibility": 100, "bestPractices": 100, "seo": 100 },
  "consoleErrors": 0,
  "brokenLinks": 0,
  "verify": {
    "checkedAt": "2026-09-23T04:16:38.651Z",
    "runs": 1,
    "words": 2696,
    "codeBlocks": 16,
    "figures": 2,
    "h1": 1,
    "mobileOverflowPx": 0,
    "consoleWarnings": 0,
    "failures": []
  },
  "screenshotsOpened": {
    "desktop-light": "fold, 03, 04 (read)",
    "desktop-dark": "fold, 03, 04",
    "mobile-light": "fold + parts 02, 03, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14 (all)",
    "mobile-dark": "fold, 09, 10, 11, 12, 13"
  },
  "justification": "node tools/run-code.mjs src/content/articles/algorithms/greedy-algorithms.md: PASS (blocks=16, executed=8). node tools/verify-page.mjs --drafts /algorithms/greedy-algorithms/ (PowerShell, serial, nothing else running): PASS, words=2696, code=16, fig=2, h1=1, Lighthouse (perf/a11y/best-practices/seo) 100/100/100/100 -- a clean sweep on all four gates. report.json: mobileOverflowPx=0, consoleErrors=[], consoleWarnings=[], broken=[], placeholders=[] (front matter already carries the real author id, so there is no NEEDS_MARKUS marker to except here). Structure: the article follows shape D exactly as CONTENT_PLAN §7 row 7 specifies -- opens with a demonstrated failure (greedy coin-change on {1,3,4} returns 3 coins where a brute-force DP check finds 2), states plainly that the greedy loop is not buggy but is the wrong tool for that input, then moves through a systematic counterexample search, the exchange-argument proof technique (CLRS ch.16, correctly cited as the source of the two-part structure), two problems where the proof goes through (interval scheduling, Huffman coding), and a closing greedy-vs-DP comparison. All five headings are H2, content-specific ('Checking whether greedy matches optimal, systematically', 'The exchange argument: proving a greedy choice is safe', etc.), no skipped levels, no boilerplate Introduction/Conclusion headings. Three exercises are well-distributed through the body (after the coin-checker section, inside interval scheduling, inside Huffman coding) and vary in form as required: 'Prove it' (exercise 1), 'Extend the code' (exercise 2), 'Predict the codes before running them' (exercise 3) -- none titled generically 'Exercises', none opening with 'Predict the output' as the very first item on the page. All three verified collapsed-by-default in both mobile-light and mobile-dark screenshots (mobile-light-part04/part08/part11), each showing only the question and a 'Show solution' disclosure triangle with zero leaked code or output above it -- the 'do not give away exercises' rule holds throughout, including for exercise 3, which is itself a predict-the-output exercise. Diagrams: Figure 1 (interval-scheduling timeline) has viewBox=\"0 0 360 320\" exactly as specified, Figure 2 (Huffman-tree build) has viewBox=\"0 0 360 300\" exactly as specified. Grep across the whole file confirms zero blank lines inside either <figure> block and zero literal fill=\"#...\"/stroke=\"#...\" values -- both diagrams use only the d-box/d-box-good/d-box-bad/d-line/d-mono/d-small classes, so they remap correctly between themes; confirmed directly in mobile-light-part07 vs mobile-dark-part09 (Figure 1) and mobile-light-part10 vs mobile-dark-part10 (Figure 2), where the good/bad box fills and strokes invert cleanly with no washed-out or illegible text in dark mode, and again in desktop-light-part04 vs desktop-dark-part04 for Figure 1. Both figures have unique title/desc ids (sched-title/sched-desc, huff-title/huff-desc), correct role=\"img\" and aria-labelledby wiring, and captions that state the exact conclusion to draw (Figure 1: which jobs are selected/rejected and why; Figure 2: the merge order and how root-to-leaf paths become codes) rather than merely describing the picture -- both figures clarify a mechanism the prose alone would need several more paragraphs to convey, not decoration. Neither figure fades at any edge in the 780px-wide (2x) mobile screenshots, meaning both sit comfortably inside the 360-wide phone budget with no horizontal scroll; on desktop (desktop-light-part03/part04) they render at a sensible size in the content column, not stretched or shrunk to an odd size. Code and output formatting: every fence uses a correct, run-code.mjs-recognized tag (```csharp run id=... for the five main worked programs, plain ```csharp run for the three exercise-solution programs, ```text output immediately following every run block with no other content between them) -- confirmed both by direct grep of all fence lines and by the run-code.mjs pass itself. I measured every text output panel's line lengths directly against the article source: the longest line in the whole article is 33 characters ({1, 5, 10, 25}  no gap up to 1000), comfortably inside the ~40-column mobile budget with margin to spare -- this confirms the writer's self-reported fix (originally ~42 characters, trimmed to ~40) actually landed with room left over, and no other output panel is close to the limit (next-longest are the two 28-character Huffman summary lines). The one ':::dotnet' callout (PriorityQueue's lack of FIFO-on-ties guarantee, and why the code packs (Frequency, Order) into TPriority to compensate) matches WRITER_GUIDE's stated purpose for the directive -- 'how the BCL or runtime really does it' -- verbatim: it quotes the official class remarks, explains the concrete consequence for this program's determinism, and is the only callout in the article, so there is no overuse. The closing three-column table (Property / Greedy / Dynamic programming) is within the 'at most three columns' guidance; at 390px it scrolls with a faded right edge showing only Property and part of Greedy by default (confirmed in mobile-light-part11/mobile-dark-part11), which is the documented, correct phone behavior for a table this wide, not a violation, and report.json's mobileOverflowPx=0 confirms the scroll is contained inside the table's own box rather than breaking page width. Rhythm of prose/code/output/callout/figure/exercise never runs long without a break across the full 14-part mobile scroll; no walls of text. Against GeeksforGeeks' greedy-algorithms hub (narrative only, no runnable code, no diagrams, no formal exchange-argument proof) and Wikipedia's Huffman-coding page (a static, unlabeled tree diagram and pseudocode, no proof of the greedy-choice property beyond a citation), this article clears both decisively: two custom, theme-correct, machine-checked-accurate diagrams; 16 runnable, verified C# blocks; and an actual two-part exchange-argument proof carried through two real problems rather than asserted. No issues were found that would cap the score below a pass.",
  "issues": []
}
