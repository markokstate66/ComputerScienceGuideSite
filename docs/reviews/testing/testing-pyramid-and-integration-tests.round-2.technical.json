{
  "article": "src/content/articles/testing/testing-pyramid-and-integration-tests.md",
  "route": "/testing/testing-pyramid-and-integration-tests/",
  "round": 2,
  "reviewer": "technical",
  "targetQuery": "unit vs integration vs end-to-end tests / testing pyramid vs testing trophy / integration test against a real database",
  "score": 9.2,
  "comparedAgainst": [
    "https://kentcdodds.com/blog/write-tests — re-fetched directly for this round. The article's quoted sentence, 'It doesn't matter if your component `<A />` renders component `<B />` with props `c` and `d` if component `<B />` actually breaks if prop `e` is not supplied,' now matches the source byte-for-byte, including the `c`/`d` prop names and the 'actually breaks if prop `e` is not supplied' clause that round 1's version had dropped/reworded. The round-1 fabrication is fully resolved. Also confirmed the post is dated 2019-07-13, matching the article's 'Kent C. Dodds' 2019 post.'",
    "https://testing.googleblog.com/2015/04/just-say-no-to-more-end-to-end-tests.html — re-fetched the raw HTML directly (not a summarizer) and grepped it: 'Google often suggests a 70/20/10 split: 70% unit tests, 20% integration tests, and 10% end-to-end tests. The exact mix will be different for each team, but in general, it should retain that pyramid shape.' appears twice in the post body (once in the main article text at the line beginning 'As a good first guess, Google often suggests...', once in a pull-quote/callout image caption reuse), both well before the comment thread starts. The article's quote is this full sentence pair verbatim, an improvement over round 1's choppy '\"70/20/10 split\"... while adding that \"the exact mix will be different...\"' fragment-quoting. Confirmed in the body, not a comment (a naive summarizer fetch of this page again returned 'I cannot find it in the body, only a commenter references it' — the same false negative round 1 flagged — so I verified against the raw HTML directly rather than trusting that tool's summary).",
    "https://learn.microsoft.com/en-us/dotnet/standard/data/sqlite/ — newly cited in this round's fix (previously an uncited source, round-1 moderate issue). Fetched directly: 'Microsoft.Data.Sqlite is a lightweight ADO.NET provider for SQLite.' The article's quoted fragment 'a lightweight ADO.NET provider for SQLite' matches verbatim, and it is now cited inline in the toolchain note where the package is first used, resolving the round-1 finding.",
    "https://xunit.net/docs/getting-started/v3/getting-started — newly cited in this round's fix (previously an uncited source, round-1 moderate issue). Fetched directly: '...unit test projects in xUnit.net v3 are stand-alone executables that can be directly run.' The article's quoted fragment 'are stand-alone executables that can be directly run' matches verbatim, and it is now cited inline explaining why the page's programs skip a .csproj and dotnet test. Resolves the round-1 finding.",
    "src/content/articles/testing/test-doubles.md (sibling article) — the round-1 'Chapter 11' misattribution (a book chapter number attached to a link pointing at this site's own chapterless web article) is gone. The sentence now reads 'The [Mocks, Stubs and Fakes](/testing/test-doubles/) article makes the general point this test relies on: a double is only as good as...' with no chapter number attributed to the linked page — matches round 1's suggested fix almost verbatim."
  ],
  "justification": "This is a re-check, not a fresh full technical pass, so I concentrated verification effort on (a) the two quote fixes named in the brief, fetched directly against live sources rather than trusted from the writer's diff, (b) the two smaller round-1 findings (uncited sources, Chapter 11 misattribution), and (c) a full re-read of the entire 604-line article for anything new. All three round-1 issues are cleanly resolved, exactly as their fixLooksLike suggested: the Dodds quote now reads 'with props `c` and `d` if component `<B />` actually breaks if prop `e` is not supplied' and I confirmed this is byte-for-byte what kentcdodds.com/blog/write-tests says today. The Google quote is now the full continuous sentence pair from the post body ('Google often suggests a 70/20/10 split: 70% unit tests, 20% integration tests, and 10% end-to-end tests. The exact mix will be different for each team, but in general, it should retain that pyramid shape.'), and I independently re-derived that it sits in the article body (grepped the raw HTML; the sentence appears at line ~1263, well before the comment thread that starts around line 1663) rather than trusting a markdown-conversion tool, which for this specific page still gives a false negative on body-vs-comment (the same trap round 1 called out). The two previously-uncited sources (Microsoft.Data.Sqlite overview, xUnit v3 getting-started) are now both cited inline in the toolchain note, and the quoted fragments attributed to each are verbatim against the live pages. The Chapter 11 misattribution is gone; the sentence now correctly attributes nothing to a chapter number and just names the sibling article. `git show` on the fix commit confirms these are the only four textual changes in the article — nothing else was touched, so the rest of round 1's extensive verification (Fowler's TestPyramid and nonDeterminism pages, the EF Core testing-strategy page, the ASP.NET Core and 'Testing in .NET' pages, the SQLite in-memory-database and datatype3 pages, xUnit's parallelism page, and the hand-traced correctness of the LinkRepository/SQLite fidelity-gap program) still holds; I re-read all of it in this pass and found nothing contradicting round 1's account. A fresh read of the full article turned up no new technical issues: complexity/behavior claims (Dictionary indexer overwrite semantics, SQLite BINARY collation, xUnit per-class collections, IClassFixture ordering) are all correct and well-scoped; the exercises' worked solutions are logically sound (the 'Find the bug before it ships' solution correctly identifies both an order-dependence failure mode and a genuine cross-class parallelism race from a shared static connection); no absolute claim ('always'/'never'/'only'/'every'/'entire') is used ungrounded — each instance found is either inside a direct quote or explicitly scoped ('the only kind that pays for a database, a network and a full running process on every run', which is true by the row above it in the comparison table). `node tools/run-code.mjs src/content/articles/testing/testing-pyramid-and-integration-tests.md` was run twice for this round, both `PASS testing/testing-pyramid-and-integration-tests.md  blocks=12 executed=6`, matching round 1's result exactly (the fix touched no code blocks, only prose). With the one disqualifying fabrication and both smaller findings resolved and no new issues surfacing on a full re-read, this clears the 8.5 technical bar: the piece is still doing real, differentiated work over its comparison sources (a genuinely executed fake-vs-real fidelity gap, two actually-reproduced flakiness demonstrations, precise sourcing throughout) rather than restating them, which is what separates it from a 'decent personal blog' treatment of the same topic. It is not a 10 — a 10 would mean clearly better than the best existing single result on every axis, and this article's value is more that it synthesizes and demonstrates rather than breaking genuinely new analytical ground versus Fowler/Dodds/Google's own pages — but it is comfortably at or above MDN/Real-Python quality with only nits remaining.",
  "issues": [
    {
      "severity": "minor",
      "location": "'So which model should you use?' section, the Google 70/20/10 citation",
      "problem": "Automated tools that render this specific Google Testing Blog page to markdown (including a fetch-and-summarize pass run during this re-check) currently mis-locate the 70/20/10 sentence as comment-only, even though it is in the post body. This is a source-side fragility (the page's HTML structure confuses simple extractors), not an article defect — the article's citation is correct — but it means a future automated re-verification of this specific claim needs a raw-HTML check rather than a summarizer, or it will produce a false 'not found in body' result.",
      "fixLooksLike": "No article change needed. Worth a one-line note for future critics/integrators re-verifying this citation: fetch the raw HTML and grep, don't trust a markdown-conversion summary for this URL.",
      "owner": "integrator"
    }
  ],
  "runCodePass": true
}
