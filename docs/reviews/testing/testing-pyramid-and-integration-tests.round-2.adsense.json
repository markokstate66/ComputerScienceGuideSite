{
  "article": "src/content/articles/testing/testing-pyramid-and-integration-tests.md",
  "route": "/testing/testing-pyramid-and-integration-tests/",
  "round": 2,
  "role": "adsense",
  "score": 9.2,
  "comparedAgainst": [
    "https://kentcdodds.com/blog/write-tests (Dodds, cited source) — re-fetched the raw page HTML directly and grepped it; every quoted fragment now attributed to Dodds ('with props c and d if component <B /> actually breaks if prop e is not supplied', 'it doesn't do you any good if you don't also verify that they work together properly', 'the confidence quotient of each form of testing increases. You get more bang for your buck', 'Integration tests strike a great balance on the trade-offs between confidence and speed/expense', 'stop mocking so much stuff') matches the source character-for-character",
    "https://testing.googleblog.com/2015/04/just-say-no-to-more-end-to-end-tests.html (Wacker/Google, cited source) — re-downloaded the raw HTML directly (not just a rendered summary) and grepped it; the post body (not a comment) contains the sentence verbatim: 'As a good first guess, Google often suggests a 70/20/10 split: 70% unit tests, 20% integration tests, and 10% end-to-end tests. The exact mix will be different for each team, but in general, it should retain that pyramid shape.' The article's quotation reproduces this continuous run of source text exactly, only dropping the leading clause 'As a good first guess, ' with no ellipsis marker",
    "https://learn.microsoft.com/en-us/dotnet/standard/data/sqlite/ (newly cited) — fetched; 'Microsoft.Data.Sqlite is a lightweight ADO.NET provider for SQLite' confirms the article's verbatim quoted fragment 'a lightweight ADO.NET provider for SQLite'",
    "https://xunit.net/docs/getting-started/v3/getting-started (newly cited) — fetched; the page's 'unit test projects in xUnit.net v3 are stand-alone executables that can be directly run' confirms the article's verbatim quoted fragment 'are stand-alone executables that can be directly run'",
    "src/content/articles/testing/test-doubles.md (sibling article) — read the passage the pyramid article quotes ('a test that uses doubles exercises a configuration that production never runs, so at least one test should cover the real assembly'); it is present verbatim in test-doubles.md's own text, attributed there to Meszaros chapter 23, and the pyramid article now correctly attributes the sentence to 'the test-doubles article' with no chapter number of its own — the round-1 'Chapter 11' misattribution does not reappear anywhere in the current text (grepped for 'Chapter', zero matches)"
  ],
  "justification": "All four round-1 blockers are fixed and I verified each against primary sources rather than trusting the round-1 summary of the fix. (1) The Dodds quote is now character-for-character verbatim, confirmed by grepping the raw kentcdodds.com HTML directly: 'with props c and d' and 'actually breaks if prop e is not supplied' are both restored exactly as the source has them. (2) The Google '70/20/10 split' quote is now a continuous, unaltered run of the source's own sentence -- I downloaded the raw HTML myself (not a summarized fetch) and found the exact string 'Google often suggests a 70/20/10 split: 70% unit tests, 20% integration tests, and 10% end-to-end tests. The exact mix will be different for each team, but in general, it should retain that pyramid shape' sitting in the post body twice (Blogger's usual desktop/mobile duplication), not in a comment. Worth noting for the record: a quick web-search-style fetch of this same URL, run through a summarizing fetcher rather than the raw HTML, initially reported back that '70/20/10' doesn't appear in the body and that it couldn't find the sentence -- that fetcher was wrong (likely fooled by the page's heavy Blogger/JS chrome); the raw HTML is unambiguous, and I am reporting what the raw HTML says. The article's quotation drops the source's leading clause 'As a good first guess, ' with no ellipsis, which is a minor, non-blocking mechanical nit (see issues), not a fabrication -- unlike round 1, nothing inside the quotation marks is altered, inserted or paraphrased. (3) Both previously-uncited sources are now genuinely cited inline, in the :::note directly before SQLite and xUnit are first used in code (not just link-dropped): 'a lightweight ADO.NET provider for SQLite' is verbatim from the Microsoft.Data.Sqlite overview, and 'are stand-alone executables that can be directly run' is verbatim from the xUnit v3 getting-started guide -- both checked against the live pages and both accurately represent what those pages say. (4) The 'Chapter 11' misattribution is gone; grepping the current file for 'Chapter' returns nothing, and the sentence it used to misattribute is now correctly sourced to the sibling test-doubles.md article (which itself attributes it, correctly, to Meszaros chapter 23) rather than claimed as a chapter of the wrong book. A fresh full read turned up nothing else that would block a pass. Originality: unchanged from round 1's strong assessment -- the LinkRepositoryTests unique-constraint suite, the RealLinkRepository/FakeLinkRepository fidelity-gap demo, and the order-dependence flakiness program remain original work with no counterpart in any cited source or in the sibling test-doubles.md article. No filler intro/outro (the opening paragraph states the article's actual content in its first sentence; there is no summary/conclusion section). No ad code, no keyword stuffing. The toolchain sentence sits in a :::note immediately before the first SQLite-backed program, not as an opening provenance stamp, matching the binding rule. Headings are question-form throughout, distinct from test-doubles.md's declarative/gerund catalog headings, so pillar structure stays varied. Four exercises (classify-the-test, extend-the-repository, predict-the-match, find-the-bug-before-it-ships) are four genuinely different forms, none opening or closing on a bare 'Predict the...', and no exercise leaks its output outside the collapsed solution. run-code.mjs passes clean (PASS, blocks=12, executed=6). The one thing keeping this below a 10 rather than blocking a pass: the 'Eradicating Non-Determinism in Tests' source (Fowler) is named and quoted from repeatedly in the flakiness section but, unlike all twelve other front-matter sources, never gets an inline markdown hyperlink back to martinfowler.com/articles/nonDeterminism.html -- it is cited by title, so this is not the 'never cited inline' failure mode the pilot rule targets, just an inconsistency with the article's own citation style everywhere else.",
  "issues": [
    {
      "severity": "minor",
      "location": "\"What makes integration tests flaky, and how do you stop it?\" section, sentence beginning \"Martin Fowler's 'Eradicating Non-Determinism in Tests' names the causes worth checking first.\"",
      "problem": "This source is cited by title and quoted from four times in this section (shared mutable state, time-dependent assertions, real network calls, resource leaks), but the title is never turned into a markdown link to https://martinfowler.com/articles/nonDeterminism.html the way every one of the other twelve front-matter sources gets an inline link at or near first use. It is not an uncited source -- the pilot rule's target -- just the one inconsistent citation in an otherwise consistent house style.",
      "fixLooksLike": "Turn the first mention into a link, e.g. Martin Fowler's [\"Eradicating Non-Determinism in Tests\"](https://martinfowler.com/articles/nonDeterminism.html) names the causes worth checking first.",
      "owner": "writer"
    },
    {
      "severity": "nit",
      "location": "\"So which model should you use?\" section, the Google testing-blog quotation",
      "problem": "The quoted sentence starts mid-source-sentence ('Google often suggests a 70/20/10 split...') and silently drops the source's own leading clause 'As a good first guess, ' without an ellipsis to mark the omission. The retained text is a fully verbatim, unaltered run from the source (confirmed against the raw page HTML), so this is a mechanical quoting nit, not a fabrication.",
      "fixLooksLike": "Either restore the leading clause ('\"As a good first guess, Google often suggests a 70/20/10 split...\"') or open the quotation with an ellipsis ('\"...Google often suggests a 70/20/10 split...\"') to mark that the quote starts mid-sentence.",
      "owner": "writer"
    }
  ],
  "runCodePass": true,
  "runCodeResult": "PASS testing/testing-pyramid-and-integration-tests.md blocks=12 executed=6 (node tools/run-code.mjs, run from Git Bash in the worktree)"
}
