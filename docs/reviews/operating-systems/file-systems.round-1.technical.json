{
  "article": "src/content/articles/operating-systems/file-systems.md",
  "route": "/operating-systems/file-systems/",
  "round": 1,
  "role": "technical",
  "score": 8.3,
  "runCodePass": true,
  "runCodeResult": "PASS operating-systems/file-systems.md  blocks=8 executed=4 (ran node tools/run-code.mjs 4x from Git Bash, identical result every time: blocks=8 executed=4, no flakiness). I additionally re-ran all four executable samples (flush-stages, writealltext-visible, atomic-replace, naive-overwrite) standalone with `dotnet run Program.cs` outside the harness, on .NET 10.0.401 on Windows 11 x64, and every program's console output matched the article's published output block byte-for-byte: 'before Flush(): 0 bytes ... after Flush(): 20 bytes ... Flush(true) returned normally'; WriteAllText visible immediately; the atomic-replace pair (3 then 5, temp file gone after each move); and the naive-overwrite pair producing output identical to the safe version's (which is the article's own point).",
  "comparedAgainst": [
    "https://github.com/dotnet/runtime/blob/main/src/libraries/System.Private.CoreLib/src/System/IO/File.cs (fetched raw source directly, both WriteToFile helper and the WriteAllText(string,string) -> WriteAllText(string,string,Encoding) -> WriteToFile(path, FileMode.Create, ...) call chain)",
    "https://learn.microsoft.com/en-us/dotnet/api/system.io.filestream.flush",
    "https://learn.microsoft.com/en-us/dotnet/api/system.io.file.writealltext",
    "https://learn.microsoft.com/en-us/dotnet/api/system.io.filemode",
    "https://learn.microsoft.com/en-us/dotnet/api/system.io.file.move",
    "https://learn.microsoft.com/en-us/dotnet/api/system.io.file.replace",
    "https://learn.microsoft.com/en-us/dotnet/api/system.io.fileoptions",
    "https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-flushfilebuffers",
    "https://learn.microsoft.com/en-us/windows/win32/fileio/master-file-table",
    "https://learn.microsoft.com/en-us/windows-server/storage/file-server/ntfs-overview",
    "https://docs.kernel.org/filesystems/ext4/overview.html (fetched raw HTML directly and grepped the body text, not just the ToC)",
    "https://docs.kernel.org/filesystems/ext4/journal.html",
    "https://docs.kernel.org/filesystems/ext4/directory.html",
    "https://docs.kernel.org/filesystems/ext4/ifork.html",
    "https://docs.kernel.org/filesystems/ext4/blockgroup.html (not in the article's source list; fetched independently to check the inode-table claim)",
    "https://man7.org/linux/man-pages/man2/fsync.2.html",
    "https://man7.org/linux/man-pages/man2/rename.2.html",
    "https://pages.cs.wisc.edu/~remzi/OSTEP/file-journaling.pdf (fetched the PDF binary and ran it through pdftotext locally, since WebFetch cannot parse PDF binaries)"
  ],
  "justification": "Code: all four `run` blocks pass node tools/run-code.mjs 4/4 with identical output (blocks=8 executed=4), and I independently compiled and ran each of the four programs myself with `dotnet run Program.cs` outside the test harness on this machine (.NET 10.0.401, Windows 11 x64) — every program's actual stdout matched the article's published output block exactly, including the subtle claims (0 bytes visible before Flush(), 20 after; WriteAllText's write visible immediately; the temp file existing then not after each SaveAtomically call; the naive-overwrite program producing output indistinguishable from the safe version's, which is the article's own pedagogical point). The code is idiomatic modern C#, uses collection-adjacent raw string literals appropriately, and each program is complete and runnable, not pseudocode. dotnet/runtime source claim: I fetched File.cs directly from github.com/dotnet/runtime and confirmed the WriteToFile private helper's exact body — it opens a SafeFileHandle via OpenHandle(path, mode, FileAccess.Write, FileShare.Read, FileOptions.None, ...), writes via RandomAccess.WriteAtOffset, and never calls Flush, Flush(true), or anything reaching FlushFileBuffers — an exact match to the article's :::dotnet callout. I also independently confirmed the full call chain WriteAllText(string,string) -> WriteAllText(string,string,Encoding) -> WriteToFile(path, FileMode.Create, contents, encoding), so the article's claim about 'WriteAllText's private helper' and its FileMode.Create behavior is accurate, not just plausible. Microsoft Learn quotes: I independently fetched and checked every quoted sentence attributed to FileStream.Flush, File.WriteAllText, FileMode, File.Move, File.Replace, FlushFileBuffers, FileOptions, and the Master File Table and NTFS overview pages — all are verbatim, word-for-word matches to the live pages (I checked each quote character-by-character against the fetched text, not just gist-matched). The NTFS cluster-size claim ('4 KB by default... scales up on very large volumes') matches the live page's cluster-size table exactly. Linux/kernel quotes: ext4's journal purpose and atomicity-guarantee quotes (journal.html), the directory-as-flat-file and inode-field quotes (directory.html), the extent-tree/four-extents-in-i_block quotes (ifork.html), fsync(2)'s directory-durability caveat, and rename(2)'s atomic-replace guarantee are all verbatim matches, independently confirmed. The OSTEP citation is real: I fetched the actual PDF and ran it through pdftotext, and it is chapter 42, titled exactly 'Crash Consistency: FSCK and Journaling,' contrasting fsck and journaling directly as the article describes. Windows-vs-Linux honesty: the distinction the article claims to maintain is real, not just asserted — the ext4/NTFS structural section explicitly flags that NTFS/C# claims are run on this (Windows) machine and ext4 claims are read from documentation (paragraph after Figure 1); the C# sections open by restating that everything from there on was actually run; and the fsync-directory-for-durable-rename caveat is correctly scoped as Linux-only, citing fsync(2) directly, and explicitly declines to guess whether NTFS has an equivalent gap ('is not something this article's Windows-only sources answer, so it is left here rather than guessed at') rather than inventing an answer — this is exactly the honest, flagged-as-open-question treatment the brief for this round asked for, and I found no place in the article where the distinction is blurred. One real issue: the claim 'every block group reserves a slice of disk called the inode table that this number indexes into' is attributed inline to '(\"ext4 Data Structures and Algorithms: Overview\")', the overview.html page. I fetched that page's raw HTML directly (not just via a summarizing fetch) and grepped its actual body prose (stripping the table-of-contents navigation, which does mention inodes only as links to other pages): the High-level Design section on that page discusses block groups only in terms of block allocation and fragmentation ('the block allocator tries very hard to keep each file's blocks within the same group') and never once mentions an inode table. The inode-table-per-block-group fact is real and correctly stated, but the page that actually documents it is blockgroup.html ('grp.bg_inode_table_*... a continuous range of blocks large enough to contain sb.s_inodes_per_group * sb.s_inode_size bytes'), which is not in the article's source list at all and was never fetched for this claim. This is exactly the pilot-round lesson the editorial brief calls out by name: 'a citation attached to a sentence its page does not support[] is a finding.' It is not a fabrication and not a wrong fact, so it does not trigger the single-factual-error score cap, but it is a real violation of the sourcing rule this whole rebuild is built on, so I am not rounding the score up past it.",
  "issues": [
    {
      "severity": "major",
      "location": "\"How does a file's inode or MFT record point to its data blocks?\" section, ext4 subsection, sentence: 'Every ext4 file has an inode number, and every block group reserves a slice of disk called the inode table that this number indexes into (\"ext4 Data Structures and Algorithms: Overview\")'",
      "problem": "The cited page (docs.kernel.org/filesystems/ext4/overview.html) does not say this. I fetched the page's raw HTML and stripped it to body text: its only prose about block groups is about block allocation and fragmentation, and the word 'inode table' does not appear anywhere on the page (the page's two mentions of 'inode' are table-of-contents links to other pages, 'Special inodes' and 'Block and Inode Allocation Policy'). The fact itself is correct and well documented on docs.kernel.org/filesystems/ext4/blockgroup.html ('The location of the inode table is given by grp.bg_inode_table_*...'), but that page is not in the article's source list and was evidently never fetched for this specific claim — a citation attached to a sentence its cited page does not support, which the editorial brief names explicitly as a pilot-round finding.",
      "fixLooksLike": "Add blockgroup.html (title something like \"ext4 Data Structures and Algorithms: Layout\") to front matter sources with an accessed date, fetch it to confirm the exact supporting language, and re-point this sentence's inline citation to it instead of to Overview.",
      "owner": "writer"
    },
    {
      "severity": "minor",
      "location": "\"What breaks if the system crashes mid-update...\" exercise, opening sentence: 'ext4's documentation says its default data=ordered mode \"only writes filesystem metadata through the journal,\" and that under it \"file data blocks are *not* guaranteed to be in any consistent state after a crash.\"'",
      "problem": "Both quotes are verbatim and accurate (confirmed against journal.html: 'For performance reasons, ext4 by default only writes filesystem metadata through the journal' and 'This means that file data blocks are /not/ guaranteed to be in any consistent state after a crash'), but unlike every other quoted claim in the article, this one has no inline source-title citation of its own — it relies on the reader inferring it's still journal.html from the paragraph two sections above. Every other direct quote in the piece names its source in the same sentence.",
      "fixLooksLike": "Add the (\"ext4 Data Structures and Algorithms: The Journal (jbd2)\") citation to this sentence, matching the article's own convention elsewhere.",
      "owner": "writer"
    }
  ]
}
