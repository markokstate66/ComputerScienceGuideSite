{
  "article": "src/content/articles/oop-design/composition-over-inheritance.md",
  "route": "/oop-design/composition-over-inheritance/",
  "round": 2,
  "role": "design",
  "score": 8.7,
  "lighthouse": { "performance": 98, "accessibility": 100, "bestPractices": 100, "seo": 100 },
  "consoleErrors": 0,
  "brokenLinks": 0,
  "verifyNotes": "Single serial run of `node tools/verify-page.mjs --drafts /oop-design/composition-over-inheritance/` from PowerShell (report.json: words=2253, codeBlocks=13, figures=2, h1=1, internalLinks=28 all OK, external=5 all OK, mobileOverflowPx=0, consoleErrors=0, consoleWarnings=0, lighthouse 98/100/100/100). Only failure is the expected NEEDS_MARKUS placeholder (x3: byline, bio, footer legal name), same as round 1 and out of scope. Opened both mobile-dark-part12.png and mobile-light-part12.png (correct parts, as the writer predicted) and read the output panel directly: all three lines -- `rejected: ArgumentNullException`, `rejected: ArgumentOutOfRangeException`, `Team plan, 5: ok` -- render fully inside the panel with no fade or clipping in either theme; the exception type names that are the evidence for the surrounding claim are completely visible. Confirmed via `git diff 31fb0b7..dd9c2ff -- src/content/articles/oop-design/composition-over-inheritance.md` that this is the only file touched (1 file changed, 3 insertions, 5 deletions) and the only hunk is the `Show` catch block's `Console.WriteLine` call plus its `text output` block (md lines ~662-685) -- both diagrams, the CS1721/silent-bug narrative, all exercises, and every other output panel are byte-identical to the version that passed round 1.",
  "comparedAgainst": [
    "https://refactoring.guru/design-patterns/composition-over-inheritance",
    "https://learn.microsoft.com/en-us/dotnet/csharp/fundamentals/object-oriented/inheritance"
  ],
  "justification": "The single major that held round 1 at 8.3 is genuinely fixed, not papered over. Screenshot evidence: both mobile-dark-part12.png and mobile-light-part12.png show the output panel with all three lines fully inside the ~40-column window (longest line is now `rejected: ArgumentOutOfRangeException` at 37 chars, versus 53 chars for the worst line before), no fade cutting into the exception-type tokens. On the pedagogical question -- can a reader still tell which bad input triggered which exception without the echoed input value -- the answer is yes, on the article's own terms. The three `Show(...)` calls (`name: null, seats: 5`; `name: \"Team plan\", seats: -2`; `name: \"Team plan\", seats: 5`) sit directly above the output panel in the same code block, in the same order the output lines print, so positional correspondence is unbroken and the reader never has to scroll or hold state across screens. More importantly, re-reading the prose that follows the block, the exception *identity* pairing (null -> ArgumentNullException, negative -> ArgumentOutOfRangeException) is not itself the point being argued -- the paragraph's two claims are about the exception hierarchy having exactly one axis and about `System.Exception` being a mandatory base rather than a swappable interface. The echoed input was flavor, not load-bearing evidence; dropping it loses nothing the surrounding argument depends on, which is a real fix rather than a workaround. One residual, sub-major observation: the code panel's own `$\"rejected: {e.GetType().Name}\");` line is 45 characters (12-space indent included) and its closing `});` still falls under the fade on a phone -- but `e.GetType().Name`, the part of the line a reader would actually care about, is fully visible before the cutoff, and WRITER_GUIDE.md's rule explicitly allows a boilerplate tail to run long (\"longer lines are fine when the tail is boilerplate; they are not fine when the tail is the point\"). This was true of the code block's shape before this change too and was correctly not flagged in round 1, so it is not treated as a new or carried-over issue here. Real Lighthouse remains excellent (98/100/100/100, up one point from round 1's 97 on performance, within normal run-to-run noise), zero console errors, zero broken links, mobileOverflowPx is 0, and the diff confirms nothing else in the article moved. Round 1's broader case for this article's design quality (purpose-built SVG figures beating both comparison sources' generic diagrams, every code block backed by a real compiled/verified output panel, three correctly-collapsed progressive exercises, clean heading hierarchy, theme-correct callouts) is untouched and still holds. This clears the 8.5 bar; the score sits at 8.7 rather than higher only because the fix is new and unreviewed by a second pass beyond this one, and the code-panel line length above is worth the writer's attention next time this article is touched even though it does not block on its own.",
  "issues": [],
  "notIssues": [
    "The code block's `Console.WriteLine($\"rejected: {e.GetType().Name}\");` line runs to 45 characters and its closing `});` falls under the mobile fade -- the visible part (`e.GetType().Name`) is the part that matters, the cut-off part is pure boilerplate, and WRITER_GUIDE.md explicitly permits this; not a repeat of the round-1 finding, which was specifically about the output panel.",
    "NEEDS_MARKUS byline/bio/footer placeholders (expected until owner supplies author identity, unchanged from round 1)",
    "draft: true (expected pre-ship)"
  ]
}
