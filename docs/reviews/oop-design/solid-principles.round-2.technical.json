{
  "article": "src/content/articles/oop-design/solid-principles.md",
  "route": "/oop-design/solid-principles/",
  "round": 2,
  "role": "technical",
  "date": "2026-09-22",
  "score": 9.3,
  "uncappedScore": 9.3,
  "runCodePass": true,
  "runCodeResult": "PASS oop-design/solid-principles.md  blocks=26 executed=14 (node tools/run-code.mjs, run from PowerShell)",
  "comparedAgainst": [
    "https://www.freecodecamp.org/news/solid-principles-explained-in-plain-english/",
    "https://en.wikipedia.org/wiki/SOLID"
  ],
  "justification": "Round 1 capped this article at 7.0 for exactly one issue: the LSP section quoted \"these mutation methods should throw\" and cited it to the IList<T> interface page [[5]], but that phrase does not appear on that page. Everything else about round 1 was already strong (uncapped 9.2). This round verifies the writer's fix and finds it correctly done, with no new problems. (1) Misquote fix, verified against the live source: the article now reads '`IList<T>.Insert`'s Exceptions list states that it throws `NotSupportedException` when \"the `IList<T>` is read-only\" [[9]]', citing a new front-matter source, the IList<T>.Insert method page. I fetched that page directly (learn.microsoft.com/en-us/dotnet/api/system.collections.generic.ilist-1.insert) and its Exceptions section reads, verbatim: 'NotSupportedException — The IList<T> is read-only.' The article's lower-cased, mid-sentence 'the IList<T> is read-only' is an accurate, properly-embedded quotation of that exact sentence — no fabrication, no added or missing words. (2) Citation [5] now carries only true, unquoted claims: I grepped every use of [[5]] in the file and found exactly two. Line 374 supports 'IList<T>.Add is a real member of that interface' (Add is indeed a listed member of the IList<T> interface page). Line 396 supports 'it inherits an IsReadOnly property from ICollection<T>' (true; IList<T> extends ICollection<T>, which declares IsReadOnly). Neither is presented as a direct quotation, and neither is the mutation-throw claim from round 1 — that claim now lives entirely under the correctly-sourced [9] citation. (3) run-code: node tools/run-code.mjs from PowerShell reports 'PASS oop-design/solid-principles.md blocks=26 executed=14', matching the expected result exactly. (4) Scope of the diff: git diff ff5817a..d6f0866 on the article touches only 9 insertions / 5 deletions, all in the front-matter sources list (one new entry, the IList<T>.Insert page) and four prose sentences (the intro Martin quote, the SRP opening sentence, the OCP opening sentence, and the LSP IsReadOnly sentence). All five violation/fix code narratives, all five csharp run blocks pairs (violation+fix each), the DIP diagram SVG, and all exercises are byte-for-byte unchanged from round 1. Beyond the flagged major, the writer also quietly tightened two other quotations I checked while re-verifying sources: the intro Martin quote had 'Separate those things that change for different reasons' (an extra 'those' not in Martin's actual sentence, which I confirmed reads 'Separate things that change for different reasons' on the Solid Relevance page) and now matches verbatim; and the DIP section's Microsoft Learn quote had 'dependency inversion lets \"A call methods on an abstraction...\"' (not verbatim — the source says 'Applying the dependency inversion principle allows A to call methods on an abstraction...') and now reads 'in the guide's own words, \"allows A to call methods on an abstraction that B implements... thus *inverting* the typical compile-time dependency\"', which I confirmed matches the architectural-principles page's actual sentence with a properly-placed ellipsis. Neither of these was flagged as an issue in round 1 (only the IList<T> quote was), so this is the writer going beyond the one required fix and cleaning up latent quote-accuracy problems in the same pass; both round-1 minors were also resolved along the way even though they were not the subject of this round's required fix: source [1] (the 2014 SRP post) is now cited inline at the article's opening SRP sentence, and the Bertrand Meyer attribution no longer leans on [[2]] for the attribution itself ('The open/closed principle is commonly traced to Bertrand Meyer; the phrasing this page uses is Martin's own summary of it' — [[2]] now only supports the quoted phrase that follows). I did not re-run AdSense or design checks this round; design passed round 1 at 9.2 and is out of scope, and AdSense is being re-checked separately.",
  "issues": [],
  "roundOneFollowUp": {
    "majorMisquote": "fixed, verified against the live IList<T>.Insert page; quote is now genuine and correctly cited to a new source [9]",
    "minorUncitedSource1": "fixed, [1] is now cited inline at the SRP section's opening sentence",
    "minorMeyerAttributionCitation": "fixed, the sentence now attributes Meyer separately from the [[2]] citation, which covers only the quoted phrase"
  }
}
