{
  "article": "csharp-dotnet/span-and-memory",
  "route": "/csharp-dotnet/span-and-memory/",
  "round": 1,
  "role": "technical",
  "score": 9.2,
  "comparedAgainst": [
    "https://learn.microsoft.com/en-us/dotnet/api/system.span-1",
    "https://learn.microsoft.com/en-us/dotnet/standard/memory-and-spans/memory-t-usage-guidelines",
    "https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/ref-struct",
    "https://blog.ndepend.com/improve-c-code-performance-with-spant/"
  ],
  "justification": "node tools/run-code.mjs src/content/articles/csharp-dotnet/span-and-memory.md passed clean (blocks=21 executed=13, runCodePass=true); node tools/check-article.mjs csharp-dotnet/span-and-memory passed with only the expected 'prerequisite not yet published' warning, which docs/crosslinks/csharp-dotnet/span-and-memory.md explains and the brief marks as not-an-issue. I fetched all four primary sources and checked the article's specific claims against them line by line: the four ref-struct restrictions (can't be boxed, can't be assigned to Object/dynamic/interface, can't be a field in a reference type, can't cross await/yield) are quoted almost verbatim from the Span<T> Remarks section and match; the C# 13 note (ref struct can implement interfaces, and 'allows ref struct' as a generic constraint, both false before C# 13) matches the ref-struct-types reference page exactly, including which restrictions were 'before C# 13' vs 'beginning with C# 13'; the two Memory<T> usage rules cited (prefer Span<T> for synchronous parameters because it 'is more versatile than Memory<T>'; use ReadOnlySpan<T>/ReadOnlyMemory<T> when the callee only reads) are Rule #1 and Rule #2 of the usage-guidelines page, correctly attributed and not closely paraphrased beyond the explicitly quoted phrase. The claim that ReadOnlySpan<T>.Split (the separator-enumerator overload used in the async example, chunk.Split((byte)'\\n')) was added in .NET 9 is correct per the MemoryExtensions.Split API reference, which lists that overload under net-9.0/10.0/11.0 monikers only (the older Span<Range> destination overload is net-8.0+, but that is not the overload the article's code calls). I hand-traced the small-buffer exercise (bufferSize: 9 against 'SKU-1042,3,19.99\\nSKU-1090,1,19.99\\n'): the first 9-byte read yields 'SKU-1042,' with the trailing comma leaving an empty 'rest', so comma2 is not found and the guard clause skips it; the next read ('3,19.99\\nS' split into '3,19.99' and 'S') also produces no valid three-field record, so total stays 0 -- exactly the article's stated output, and the article's diagnosis (a protocol/framing bug, not a Span/Memory lifetime bug) is the correct root cause. All four ref-struct compiler-error blocks (CS0029, CS8175, CS8345, CS4007) were actually compiled by run-code.mjs, and the CS8345 message text quoted in prose ('unless it is an instance member of a ref struct') matches the real compiler diagnostic for that error. The allocation numbers (1,152,000 B / 8,000 lines and 5,760,000 B / 40,000 lines, both exactly 144 B/line; 160 B vs 0 B for the stackalloc formatting example; 0 B at 100,000 iterations) are machine-measured via GC.GetAllocatedBytesForCurrentThread and reproduced by the harness, and the article correctly derives O(n) scaling from the 5x-for-5x-input ratio without overclaiming precision. Depth and rigor clearly exceed the blog-tier competitor I read (NDepend's Span<T> post, which covers similar ground with a single BenchmarkDotNet number and no reproduced compiler errors, no ref-struct-restriction enumeration, and no async/Memory<T> pattern), and the article combines and goes beyond what the two Microsoft Learn pages cover separately (it reproduces the restrictions as actual compile failures rather than just listing them, and builds a complete allocation-free async parser the docs only sketch conceptually). I found no fabricated claims, no uncited generalizations, no unscoped 'always/never' language, and no factual errors, so nothing caps the score below the code/citation ceiling. The 0.8 held back from a 10 is stylistic headroom (I read this as fully sourced and correct, not as flawless prose I'd call strictly unimprovable) rather than any identified defect.",
  "issues": [],
  "reviewFileWritten": "C:\\Users\\marko\\AppData\\Local\\Temp\\csg-worktrees\\issue-126-span-and-memory\\docs\\reviews\\csharp-dotnet\\span-and-memory.round-1.technical.json",
  "runCodePass": true
}
