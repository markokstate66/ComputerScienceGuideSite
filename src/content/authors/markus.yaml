# The single site author. `name` uses the publication's own name rather than a specific person's
# name until the owner answers docs/NEEDS_MARKUS.md item #1 -- this is not a guess at a real name,
# it's an honest "unnamed author, named publication" stand-in, a normal pattern for a site before
# a byline is finalized. `bio` is intentionally empty until #2 is answered; every template that
# reads author.data.bio must skip rendering it rather than print an empty paragraph.
name: "Computer Science Guide"
bio: ""
links: []
