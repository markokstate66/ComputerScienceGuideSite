---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="500 - Server Error"
  description="Something went wrong on our end."
  noindex={true}
>
  <section class="error-page">
    <div class="container">
      <div class="error-art">
        <pre class="ascii-art">{`
    (╯°□°)╯︵ ┻━┻
        `}</pre>
      </div>
      <h1>500</h1>
      <p class="tagline">Unhandled exception in production</p>
      <div class="terminal">
        <code>
          <span class="error-line">Error: Something went catastrophically wrong</span><br/>
          <span class="stack">&nbsp;&nbsp;at Server.handleRequest (server.js:42)</span><br/>
          <span class="stack">&nbsp;&nbsp;at processTicksAndRejections (internal)</span><br/>
          <span class="stack">&nbsp;&nbsp;at async Universe.maintainOrder (reality.js:1)</span><br/>
          <span class="prompt">$</span> <span class="cursor">_</span>
        </code>
      </div>
      <p class="message">
        Our server threw an exception and forgot to catch it.
        We're blaming it on cosmic rays flipping bits, but honestly, it's probably our fault.
      </p>
      <div class="status-box">
        <span class="status-icon">&#9881;</span>
        <span>Our engineers have been notified (or will be, once they check their alerts)</span>
      </div>
      <div class="actions">
        <a href="/" class="btn">Try Again</a>
        <a href="/guides" class="btn btn-secondary">Browse Guides</a>
      </div>
      <p class="hint">In the meantime, have you tried turning it off and on again?</p>
    </div>
  </section>

  <style>
    .error-page {
      text-align: center;
      padding: 4rem 0 6rem;
      min-height: 60vh;
      display: flex;
      align-items: center;
    }

    .container {
      width: 100%;
    }

    .error-art {
      margin-bottom: 1rem;
    }

    .ascii-art {
      font-family: var(--font-mono);
      font-size: 1.5rem;
      color: #ef4444;
      background: transparent;
      display: inline-block;
      line-height: 1.2;
    }

    h1 {
      font-size: 6rem;
      background: linear-gradient(135deg, #ef4444 0%, #f97316 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin: 0;
      line-height: 1;
    }

    .tagline {
      font-family: var(--font-mono);
      font-size: 1.1rem;
      color: var(--color-text-muted);
      margin: 0.5rem 0 1.5rem;
    }

    .terminal {
      background: var(--color-code-bg);
      color: var(--color-code-text);
      padding: 1.5rem 2rem;
      border-radius: 8px;
      display: inline-block;
      text-align: left;
      margin: 1.5rem 0;
      font-family: var(--font-mono);
      font-size: 0.85rem;
    }

    .error-line {
      color: #ef4444;
    }

    .stack {
      color: var(--color-text-muted);
    }

    .prompt {
      color: var(--color-secondary);
    }

    .cursor {
      animation: blink 1s infinite;
    }

    @keyframes blink {
      0%, 50% { opacity: 1; }
      51%, 100% { opacity: 0; }
    }

    .message {
      font-size: 1.1rem;
      color: var(--color-text-muted);
      max-width: 550px;
      margin: 1.5rem auto;
    }

    .status-box {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      background: var(--color-bg-alt);
      border: 1px solid var(--color-border);
      padding: 0.75rem 1.25rem;
      border-radius: 8px;
      font-size: 0.9rem;
      color: var(--color-text-muted);
      margin: 1rem 0;
    }

    .status-icon {
      font-size: 1.1rem;
      animation: spin 3s linear infinite;
    }

    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .actions {
      margin-top: 2rem;
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn-secondary {
      background: transparent;
      border: 2px solid var(--color-primary);
      color: var(--color-primary);
    }

    .btn-secondary:hover {
      background: var(--color-primary);
      color: white;
    }

    .hint {
      font-size: 0.875rem;
      color: var(--color-text-muted);
      margin-top: 3rem;
      opacity: 0.7;
    }

    @media (max-width: 768px) {
      h1 {
        font-size: 4rem;
      }

      .ascii-art {
        font-size: 1.2rem;
      }

      .terminal {
        padding: 1rem;
        font-size: 0.75rem;
      }

      .status-box {
        font-size: 0.8rem;
      }
    }
  </style>
</BaseLayout>
