---
description: Alias for /work-next — pick the highest-priority `todo` issue (or a given #), work it on a branch, open a PR that closes it
argument-hint: "[optional: issue number]"
allowed-tools: Bash, PowerShell, Read, Edit, Write, Grep, Glob, Agent, WebSearch, WebFetch, AskUserQuestion
---

`/next` is a shorthand alias for `/work-next`.

Read `.claude/commands/work-next.md` and execute those instructions **exactly**, using the following as its "Requested issue (optional)" argument (may be empty, meaning: pick the top of the queue):

**$ARGUMENTS**
